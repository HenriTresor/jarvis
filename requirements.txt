# J.A.R.V.I.S. Requirements
# Install with: pip install -r requirements.txt

# Voice Pipeline (Wake Word, STT, TTS)
openwakeword
faster-whisper
TTS
piper-tts
sounddevice
pyaudio
scipy
numpy

# Memory (Vector DB + Structured Facts)
chromadb

# LLM (Groq cloud inference)
groq

# Agent & Tools (Web Search, Weather, etc.)
ddgs
requests
python-dotenv

# Vision (Image Analysis, Motion Detection)
opencv-python
Pillow

# API Backend (FastAPI + WebSocket)
fastapi
uvicorn
pydantic
websockets
aiofiles
python-multipart

# Optional: Google APIs (Gmail, Calendar)
# google-auth
# google-auth-oauthlib
# google-api-python-client

# Optional: Task Queue (Celery + Redis)
# celery
# redis
openai>=1.0.0
